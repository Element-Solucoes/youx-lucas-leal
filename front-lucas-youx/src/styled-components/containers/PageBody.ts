import styled from 'styled-components';

// Este container só é utilizado uma vez, englobando toda a página.

export const PageBody = styled.div`
  min-width: 100vw;
  max-width: 100vw;
  min-height: 100vh;
  overflow-x: hidden;
`;
