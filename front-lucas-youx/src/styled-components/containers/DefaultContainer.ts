import styled from 'styled-components';

// Um container básico e padrão para deixar a página um pouco mais centralizada.

export const DefaultContainer = styled.section`
  width: 90%;
  margin-left: auto;
  margin-right: auto;
  min-height: 100vh;
  position: relative;
  z-index: 2;
`;
