import styled from 'styled-components';

// O textinho que aparece embaixo dos formulários mostrando um feedback.

export const FormFeedback = styled.p`
  max-width: 15em;
  text-align: center;
  font-size: 0.8em;
  margin: 1em auto 1em auto;
  color: #6b00ff;
`;
